# ============================================================
# Atlas TA — Bloque 1: Visor de mapas (muestras + consultores)
# ============================================================

# ── Core Streamlit ───────────────────────────────────────────
streamlit==1.37.0
streamlit-folium==0.10.0

# ── Data / NumPy stack ───────────────────────────────────────
numpy==1.25.0
pandas==1.5.3
plotly==5.14.0

# ── Mapas / Geo ──────────────────────────────────────────────
folium>=0.15.0
shapely==2.0.1
pyproj>=3.6.0
geopandas>=0.14.0

# ── ML / Clustering (metricas_areas.py) ─────────────────────
scikit-learn==1.3.0
joblib>=1.3.0

# ── Base de datos ────────────────────────────────────────────
SQLAlchemy>=2.0.0
# PyMySQL: driver pure Python — reemplaza C extension de mysql-connector
# que en v9.x causa segfault silencioso con SQLAlchemy en Streamlit threaded.
# Instalar: pip install PyMySQL
PyMySQL>=1.0.0
# mysql-connector-python pinned a 8.0.33 (último estable sin C extension bugs).
# Se usa solo si PyMySQL no está disponible (ver pre_procesamiento/db_utils.py).
mysql-connector-python==8.0.33

# ── Seguridad / Config ───────────────────────────────────────
python-dotenv>=1.0.0
cryptography>=42.0.0
validators==0.34.0

# ── Servidor Flask (flask_server.py) ─────────────────────────
flask==2.2.5
flask-cors==5.0.0
werkzeug==2.2.3

# ── UI helpers ───────────────────────────────────────────────
Pillow>=10.0.0

# ── Agente conversacional / Vanna SQL ────────────────────────
openai>=1.30.0
vanna>=0.7.4
chromadb>=0.5.0
# Embedding semántico para ChromaDB (retrieval real vs hash puro).
# Si no puede instalarse (ej. onnxruntime ABI en Windows), atlas_vanna.py
# cae automáticamente al embedding hash sin romper nada.
sentence-transformers>=2.7.0
